mixin pageBase(options = {})
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    meta(name="keywords" content=options.keywords)
    meta(name="description" content=options.description)
    title=options.title
